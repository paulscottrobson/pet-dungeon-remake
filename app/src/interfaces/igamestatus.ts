/// <reference path="../../lib/phaser.comments.d.ts"/>

interface IGameStatus {
    x:number;
    y:number;
    level:number;
    gold:number;
    experience:number;
    hitPoints:number;
    lastExperience:number;
    killCount:number;
}
